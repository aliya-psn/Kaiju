# 聚会趣味游戏 · 微信小程序

面对面聚会小游戏，包括谁是卧底、酒筛子、海龟汤等类目。

## 功能

### 面板页
- 展示所有游戏类目（谁是卧底、酒筛子、海龟汤）
- 卡片式布局，支持点击跳转
- 使用 `svg/` 目录下的人物头像展示

### 谁是卧底 · 词库自动生成
- **痛点**：玩多了词库背熟，失去新鲜感
- **解法**：内置 100+ 主题词库
- **主题**：职场黑话、明星梗、网络流行语、美食饮品、动物世界、日常生活、科技数码、情绪表情、体育运动、旅行出游
- **亮点**：永远玩不腻，每次都有新词
- 词库维护：`data/wordLib.js`，后期可定期更新（注：小程序不支持 require 加载 .json，使用 .js 导出）

## 项目结构

```
kaiju/
├── app.js / app.json / app.wxss
├── pages/
│   ├── index/          # 面板页
│   └── wordgen/        # 谁是卧底词库生成
├── data/
│   ├── wordLib.js      # 词库数据（可编辑扩展）
│   └── wordLib.json    # 备份/参考用
└── svg/                # 人物头像
    ├── avatar_1.svg
    ├── avatar_2.svg
    └── ...
```

## 开发

1. 用微信开发者工具打开项目
2. 选择 `project.config.json` 所在目录
3. 编译运行

## 词库扩展

编辑 `data/wordLib.js`：
- 新增分类：在 `categories` 数组添加对象，包含 `id`、`name`、`icon`、`description`、`wordPairs`
- 新增词对：在对应分类的 `wordPairs` 添加 `['平民词', '卧底词']` 数组
